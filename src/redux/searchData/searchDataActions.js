import { createAction } from "@reduxjs/toolkit";

const writeSearchData = createAction("SEARCH_DATA_writeSearchData");

// eslint-disable-next-line import/no-anonymous-default-export
export default {
    writeSearchData,
};
